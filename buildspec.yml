version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install

  build:
    commands:
      - node -v
      - npm -v
      # Do not run the server in build phase unless required
      # This is just build, not start

  post_build:
    commands:

artifacts:
  files:
    - '**/*'
